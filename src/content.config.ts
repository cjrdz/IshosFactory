// Content collections configuration
// We're using regular JSON imports instead of content collections for menu data
import { defineCollection } from 'astro:content';

// Define an empty collections object to prevent auto-generation warnings
export const collections = {};


