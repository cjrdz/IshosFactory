---
import Icon from "../svelte/Icon.svelte";
import configData from "../../content/config.json";

const whatsappNumber = configData.whatsappNumber || "50312345678";
const phoneNumber = whatsappNumber.replace(/\D/g, '').startsWith('503') 
    ? whatsappNumber.replace(/\D/g, '') 
    : `503${whatsappNumber.replace(/\D/g, '')}`;
---

<div class="container mx-auto px-4 py-6 space-y-12">
    <!-- Acerca de Isho's Factory Section -->
    <section class="max-w-4xl mx-auto">
        <h2 class="text-4xl font-bold text-center mb-8 flex items-center justify-center gap-2">
            <Icon icon="mdi:heart" width="32" height="32" client:load />
            Isho's Factory
        </h2>
        
        <div class="card bg-base-100 shadow-xl border border-base-300 rounded-2xl p-8">
            <div class="card-body">
                <p class="text-lg text-base-content mb-6 leading-relaxed">
                    Desde 2021, Isho's Factory ha sido sinónimo de tradición y calidad en El Salvador. 
                    Nuestros helados artesanales son preparados con los mejores ingredientes y mucho amor. 
                    Cada bola de helado cuenta una historia de sabor auténtico y dedicación. 
                    Visítanos y descubre por qué generaciones de salvadoreños nos eligen.
                </p>
                
                <div class="flex flex-wrap gap-4 justify-center">
                    <div class="badge badge-lg badge-info gap-2 px-4 py-3 text-base">
                        <Icon icon="mdi:hand-coin" width="20" height="20" client:load />
                        Artesanal
                    </div>
                    <div class="badge badge-lg badge-secondary gap-2 px-4 py-3 text-base">
                        <Icon icon="mdi:leaf" width="20" height="20" client:load />
                        Ingredientes Naturales
                    </div>
                    <div class="badge badge-lg badge-primary gap-2 px-4 py-3 text-base">
                        <Icon icon="mdi:clock-outline" width="20" height="20" client:load />
                        Desde 2021
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contáctanos Section -->
    <section class="max-w-4xl mx-auto">
        <h2 class="text-4xl font-bold text-center mb-8 flex items-center justify-center gap-2">
            <Icon icon="mdi:map-marker" width="32" height="32" client:load />
            Contáctanos
        </h2>
        
        <div class="card bg-base-100 shadow-xl border border-base-300 rounded-2xl p-8">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <!-- Visítanos -->
                <div class="text-center">
                    <div class="flex justify-center mb-4">
                        <Icon icon="mdi:store" width="48" height="48" class="text-primary" client:load />
                    </div>
                    <h3 class="text-xl font-bold text-primary mb-2">Visítanos</h3>
                    <p class="text-base-content/70">El Salvador<br />Ahuachapán</p>
                </div>
                
                <!-- Llámanos -->
                <div class="text-center">
                    <div class="flex justify-center mb-4">
                        <Icon icon="mdi:phone" width="48" height="48" class="text-primary" client:load />
                    </div>
                    <h3 class="text-xl font-bold text-primary mb-4">Llámanos</h3>
                    <div class="flex flex-col gap-3">
                        <a 
                            href={`tel:+${phoneNumber}`}
                            class="btn btn-primary btn-sm gap-2"
                        >
                            <Icon icon="mdi:phone" width="20" height="20" client:load />
                            Llamar
                        </a>
                        <a 
                            href={`https://wa.me/${phoneNumber}`}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="btn btn-success btn-sm gap-2"
                        >
                            <Icon icon="mdi:whatsapp" width="20" height="20" client:load />
                            WhatsApp
                        </a>
                    </div>
                </div>
                
                <!-- Horario -->
                <div class="text-center">
                    <div class="flex justify-center mb-4">
                        <Icon icon="mdi:clock-outline" width="48" height="48" class="text-primary" client:load />
                    </div>
                    <h3 class="text-xl font-bold text-primary mb-2">Horario</h3>
                    <p class="text-base-content/70 text-sm">
                        Lun - Viernes<br />
                        10:30 AM - 6:30 PM
                    </p>
                    <p class="text-base-content/70 text-sm mt-2">
                        Domingo<br />
                        10:30 AM - 6:30 PM
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Síguenos en Redes Sociales Section -->
    <section class="max-w-4xl mx-auto">
        <h2 class="text-4xl font-bold text-center mb-8 flex items-center justify-center gap-2">
            <Icon icon="mdi:share-variant" width="32" height="32" client:load />
            Síguenos en Redes Sociales
        </h2>
        
        <div class="card bg-base-100 shadow-xl border border-base-300 rounded-2xl p-8">
            <div class="flex flex-wrap justify-center gap-6">
                <!-- Instagram -->
                <a 
                    href="https://www.instagram.com/ishos_factory/?hl=en" 
                    target="_blank" 
                    rel="noopener noreferrer"
                    class="flex flex-col items-center justify-center gap-2 w-24 h-24 rounded-xl bg-gradient-to-br from-purple-600 via-pink-600 to-orange-500 hover:scale-110 transition-transform shadow-md"
                >
                    <Icon icon="mdi:instagram" width="32" height="32" class="text-white" client:load />
                    <span class="text-white text-xs font-semibold">Instagram</span>
                </a>
                
                <!-- Facebook -->
                <a 
                    href="https://www.facebook.com/ishosfactory/" 
                    target="_blank" 
                    rel="noopener noreferrer"
                    class="flex flex-col items-center justify-center gap-2 w-24 h-24 rounded-xl bg-blue-600 hover:scale-110 transition-transform shadow-md"
                >
                    <Icon icon="mdi:facebook" width="32" height="32" class="text-white" client:load />
                    <span class="text-white text-xs font-semibold">Facebook</span>
                </a>
                
                <!-- TikTok -->
                <a 
                    href="https://www.tiktok.com/@ishosfactory" 
                    target="_blank" 
                    rel="noopener noreferrer"
                    class="flex flex-col items-center justify-center gap-2 w-24 h-24 rounded-xl bg-black hover:scale-110 transition-transform shadow-md"
                >
                    <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="text-white">
                        <path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-5.2 1.74 2.89 2.89 0 0 1 2.31-4.64 2.93 2.93 0 0 1 .88.13V9.4a6.84 6.84 0 0 0-1-.05A6.33 6.33 0 0 0 5 20.1a6.34 6.34 0 0 0 10.86-4.43v-7a8.16 8.16 0 0 0 4.77 1.52v-3.4a4.85 4.85 0 0 1-1-.1z" fill="currentColor"/>
                    </svg>
                    <span class="text-white text-xs font-semibold">TikTok</span>
                </a>
            </div>
        </div>
    </section>
</div>
